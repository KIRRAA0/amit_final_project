import 'package:flutter/material.dart';

class UploadPortfolioPage extends StatelessWidget {
  const UploadPortfolioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Upload Portfolio Page'),
    );
  }
}