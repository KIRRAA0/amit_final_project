import 'package:flutter/material.dart';

class TypeOfWorkPage extends StatelessWidget {
  const TypeOfWorkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Type of Work Page'),
    );
  }
}
